This is the Swift Package version of https://github.com/player-ui/devtools. This is automatically kept in sync with devtools and no development should happen directly in this repo.

To leverage this package, add this to your Package.swift and change to use the appropriate version:

```swift
.package(url: "https://github.com/player-ui/devtools-ios.git", from: "0.1.0")
```